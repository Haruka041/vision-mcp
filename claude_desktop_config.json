{
  "mcpServers": {
    "mcp-vision-server": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "-p", "8080:8080",
        "-v", "${workspaceFolder}/mcp_data:/data",
        "mcp-vision-server"
      ]
    }
  }
}
